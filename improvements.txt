Act as a senior UI/UX designer and professional frontend developer.

I have a homepage for a handmade natural soap website. The HTML structure, SEO, and accessibility are already implemented, but the website lacks visual appeal and polish.

Your task is to improve the visual design while keeping the existing structure intact.

🎯 Design Goals

Premium, clean, handmade, organic look

Natural & eco-friendly brand feel

Modern but not flashy

Mobile-first and conversion-focused

🧱 Areas to Improve (WITH EXAMPLES)
1. Hero Section

Improve the hero section with a strong visual impact.

What to do:

Add a background image or gradient overlay

Improve text hierarchy

Add a secondary CTA

Example CSS:

.hero {
  min-height: 85vh;
  display: flex;
  align-items: center;
  background:
    linear-gradient(rgba(0,0,0,.45), rgba(0,0,0,.45)),
    url('/assets/img/hero-soap.jpg') center/cover;
  color: #fff;
}

2. Color System

Create a consistent color palette using CSS variables.

Example:

:root {
  --primary: #9CAF88;
  --secondary: #F6F5F1;
  --accent: #C28E5C;
  --text-dark: #2B2B2B;
}


Use these variables consistently across buttons, backgrounds, and text.

3. Typography

Improve typography using a professional font pairing.

Example:

body {
  font-family: 'Inter', sans-serif;
}

h1, h2, h3 {
  font-family: 'Playfair Display', serif;
}


Improve font sizes, spacing, and line heights.

4. Buttons & CTAs

Make buttons feel premium.

Example:

.btn-primary {
  background: var(--accent);
  padding: 0.9rem 2rem;
  border-radius: 999px;
  font-weight: 600;
  box-shadow: 0 10px 25px rgba(0,0,0,.15);
  transition: all .2s ease;
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 15px 35px rgba(0,0,0,.25);
}

5. Cards (Products & Features)

Use card layouts instead of flat sections.

Example:

.card {
  background: #fff;
  border-radius: 16px;
  padding: 1.5rem;
  box-shadow: 0 15px 40px rgba(0,0,0,.08);
  transition: transform .25s ease;
}

.card:hover {
  transform: translateY(-6px);
}

6. Spacing & Layout

Increase white space to improve readability.

Example:

section {
  padding: 6rem 0;
}

.section-title {
  margin-bottom: 3rem;
}

7. Trust & Branding Sections

Add visual trust elements.

Example Content:

100% Natural Ingredients

Handmade in Small Batches

Eco-Friendly Packaging

Use icons and short descriptions.

📤 Output Requirements

Provide CSS improvements

Suggest layout changes

Keep changes realistic and implementable

Do NOT rewrite the entire HTML